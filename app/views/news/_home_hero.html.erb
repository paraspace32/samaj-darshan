<%# ═══════════════════════════════════════════════════════════════════
    HOME HERO — featured lead story + side stack
    Requires: @featured, @featured_type, @home_side_items, @biodata_count
    ═══════════════════════════════════════════════════════════════════ %>
<% if @featured.present? %>
  <%
    featured_path = case @featured_type
                    when :education then education_path(@featured)
                    when :job then job_path(@featured)
                    else news_path(@featured)
                    end
    featured_content = case @featured_type
                       when :job then @featured.display_description
                       else @featured.display_content
                       end
    featured_badge_label = case @featured_type
                           when :education then t("education.title")
                           when :job then t("feed.job_news")
                           else @featured.region.display_name
                           end
    featured_badge_color = "#f97316"
    featured_sub_label = case @featured_type
                         when :education then t("education.title")
                         when :job then @featured.company_name
                         else @featured.region.display_name
                         end
  %>

  <%# ── Mobile Hero: title-first card layout ── %>
  <div class="sm:hidden -mx-4 mb-2 animate-fade-up">
    <%= link_to featured_path, class: "group block bg-orange-50/60 rounded-xl overflow-hidden" do %>
      <%# 1. Heading at top %>
      <div class="px-4 pt-2.5 pb-1.5">
        <h2 class="text-[1rem] font-black text-gray-900 leading-[1.3] line-clamp-3 tracking-tight group-hover:text-orange-600 transition-colors">
          <%= @featured.display_title %>
        </h2>
      </div>
      <%# 2. Image with pulse dot %>
      <div class="w-full aspect-[2/1] overflow-hidden relative">
        <% if @featured.cover_image.attached? %>
          <%= optimized_image_tag @featured.cover_image, variant: :hero, class: "w-full h-full object-cover group-hover:scale-[1.02] transition-transform duration-500", loading: "eager" %>
        <% else %>
          <div class="w-full h-full gradient-hero"></div>
        <% end %>
        <div class="absolute top-2.5 left-3">
          <span class="relative flex items-center justify-center h-3 w-3">
            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-orange-400 opacity-75"></span>
            <span class="relative inline-flex rounded-full h-1.5 w-1.5 bg-orange-500 shadow-md shadow-black/20"></span>
          </span>
        </div>
      </div>
      <%# 3. Content excerpt (2 lines) %>
      <div class="px-4 pt-2 pb-2.5">
        <p class="text-[13px] text-gray-600 leading-relaxed line-clamp-2"><%= truncate(strip_tags(featured_content), length: 160) %></p>
      </div>
    <% end %>

    <%# Mobile side items — rows of 2, banners between %>
    <% row1 = @home_side_items.first(2) %>
    <% mid_items = @home_side_items.drop(2).first(4) %>
    <div class="story-stack mt-2">
      <% row1.each do |item| %>
        <%= link_to commentable_path(item), class: "group story-card bg-orange-50/60 overflow-hidden" do %>
          <div class="w-full aspect-[4/3] overflow-hidden">
            <% if item.cover_image.attached? %>
              <%= optimized_image_tag item.cover_image, variant: :card, class: "w-full h-full object-cover group-hover:scale-[1.02] transition-transform duration-500", loading: "lazy" %>
            <% else %>
              <div class="w-full h-full gradient-brand-soft"></div>
            <% end %>
          </div>
          <div class="p-2.5 bg-orange-50/60">
            <h3 class="text-[13px] font-bold text-gray-900 group-hover:text-orange-600 transition-colors line-clamp-3 leading-snug tracking-tight"><%= item.display_title %></h3>
          </div>
        <% end %>
      <% end %>
    </div>

    <%# विवाह परिचय banner %>
    <%= link_to biodatas_path, class: "block group my-2 mx-4" do %>
      <div class="rounded-xl relative overflow-hidden text-white px-4 py-3 flex items-center gap-3"
           style="background: linear-gradient(150deg,#6b2009 0%,#b83a0a 35%,#ea580c 65%,#fb923c 100%); box-shadow: 0 6px 28px -6px rgba(180,83,9,0.55); border: 1.5px solid rgba(245,200,151,0.25)">
        <div class="absolute top-0 left-0 right-0 h-[3px] pointer-events-none" style="background: linear-gradient(90deg,#7c2d12,#c2410c,#ea580c,#f97316,#ea580c,#c2410c,#7c2d12)"></div>
        <div class="absolute inset-0 overflow-hidden pointer-events-none" aria-hidden="true">
          <div class="absolute -top-8 -left-8 w-40 h-40 rounded-full" style="border: 16px solid rgba(255,255,255,0.08)"></div>
          <div class="absolute -bottom-12 left-20 w-32 h-32 rounded-full" style="border: 12px solid rgba(255,255,255,0.06)"></div>
          <div class="absolute top-1/2 -translate-y-1/2 -right-8 w-28 h-28 rounded-full" style="border: 10px solid rgba(255,255,255,0.07)"></div>
        </div>
        <div class="w-10 h-10 rounded-xl flex items-center justify-center shrink-0 relative z-10"
             style="background: rgba(255,255,255,0.15); box-shadow: 0 0 18px rgba(255,255,255,0.1)">
          <svg class="w-5 h-5 text-white" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 21.593c-5.63-5.539-11-10.297-11-14.402 0-3.791 3.068-5.191 5.281-5.191 1.312 0 4.151.501 5.719 4.457 1.59-3.968 4.464-4.447 5.726-4.447 2.54 0 5.274 1.621 5.274 5.181 0 4.069-5.136 8.625-11 14.402z"/>
          </svg>
        </div>
        <div class="flex-1 min-w-0 relative z-10">
          <div class="flex flex-wrap items-center gap-2 mb-0.5">
            <h3 class="font-black text-[13px] leading-tight"><%= t("nav.marriage") %></h3>
            <% if @biodata_count&.positive? %>
              <span class="text-[11px] font-bold px-2 py-0.5 rounded-full whitespace-nowrap" style="background: rgba(255,255,255,0.18); border: 1px solid rgba(255,255,255,0.25)">
                <%= @biodata_count %>+ <%= I18n.locale == :hi ? "प्रोफाइल" : "Profiles" %>
              </span>
            <% end %>
          </div>
          <p class="text-[11px] leading-snug" style="color: rgba(255,255,255,0.65)"><%= t("feed.marriage_ads_subtitle") %></p>
        </div>
        <svg class="w-4 h-4 shrink-0 relative z-10 group-hover:translate-x-0.5 transition-transform" style="color:rgba(255,255,255,0.45)" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
          <path stroke-linecap="round" d="M9 5l7 7-7 7"/>
        </svg>
      </div>
    <% end %>

    <%# Middle cards — between marriage and tribute banners %>
    <% if mid_items.any? %>
      <div class="story-stack">
        <% mid_items.each do |item| %>
          <%= link_to commentable_path(item), class: "group story-card bg-orange-50/60 overflow-hidden" do %>
            <div class="w-full aspect-[4/3] overflow-hidden">
              <% if item.cover_image.attached? %>
                <%= optimized_image_tag item.cover_image, variant: :card, class: "w-full h-full object-cover group-hover:scale-[1.02] transition-transform duration-500", loading: "lazy" %>
              <% else %>
                <div class="w-full h-full gradient-brand-soft"></div>
              <% end %>
            </div>
            <div class="p-2.5 bg-orange-50/60">
              <h3 class="text-[13px] font-bold text-gray-900 group-hover:text-orange-600 transition-colors line-clamp-3 leading-snug tracking-tight"><%= item.display_title %></h3>
            </div>
          <% end %>
        <% end %>
      </div>
    <% end %>

    <%# श्रद्धांजलि / Tribute banner %>
    <%= link_to tributes_path, class: "block group my-2 mx-4" do %>
      <div class="rounded-xl relative overflow-hidden text-white px-4 py-3 flex items-center gap-3"
           style="background: linear-gradient(150deg,#831843 0%,#be185d 35%,#ec4899 65%,#f9a8d4 100%); box-shadow: 0 6px 28px -6px rgba(190,24,93,0.55); border: 1.5px solid rgba(249,168,212,0.25)">
        <div class="absolute top-0 left-0 right-0 h-[3px] pointer-events-none" style="background: linear-gradient(90deg,#831843,#be185d,#ec4899,#f472b6,#ec4899,#be185d,#831843)"></div>
        <div class="absolute inset-0 overflow-hidden pointer-events-none" aria-hidden="true">
          <div class="absolute -top-8 -left-8 w-40 h-40 rounded-full" style="border: 16px solid rgba(255,255,255,0.08)"></div>
          <div class="absolute -bottom-12 left-20 w-32 h-32 rounded-full" style="border: 12px solid rgba(255,255,255,0.06)"></div>
          <div class="absolute top-1/2 -translate-y-1/2 -right-8 w-28 h-28 rounded-full" style="border: 10px solid rgba(255,255,255,0.07)"></div>
        </div>
        <div class="w-10 h-10 rounded-xl flex items-center justify-center shrink-0 relative z-10"
             style="background: rgba(255,255,255,0.15); box-shadow: 0 0 18px rgba(255,255,255,0.1)">
          <span class="text-xl">🌸</span>
        </div>
        <div class="flex-1 min-w-0 relative z-10">
          <h3 class="font-black text-[13px] leading-tight"><%= t("nav.tributes") %></h3>
          <p class="text-[11px] leading-snug" style="color: rgba(255,255,255,0.65)"><%= t("tributes.subtitle") %></p>
        </div>
        <svg class="w-4 h-4 shrink-0 relative z-10 group-hover:translate-x-0.5 transition-transform" style="color:rgba(255,255,255,0.45)" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
          <path stroke-linecap="round" d="M9 5l7 7-7 7"/>
        </svg>
      </div>
    <% end %>

  </div>

  <%# ── Desktop: side items row above hero ── %>
  <% if @home_side_items.present? %>
    <div class="hidden sm:grid sm:grid-cols-4 gap-3 lg:gap-4 mb-3 lg:mb-4 animate-fade-up">
      <% @home_side_items.first(4).each_with_index do |item, idx| %>
        <%= link_to commentable_path(item), class: "group block rounded-xl overflow-hidden bg-white border border-gray-100 hover:border-orange-200 hover:shadow-md transition-all card-lift" do %>
          <div class="relative h-28 lg:h-32 overflow-hidden">
            <% if item.cover_image.attached? %>
              <%= optimized_image_tag item.cover_image, variant: :card, class: "w-full h-full object-cover img-zoom", loading: idx < 2 ? "eager" : "lazy" %>
            <% else %>
              <div class="w-full h-full gradient-brand-soft"></div>
            <% end %>
          </div>
          <div class="p-2.5">
            <h3 class="text-[12px] font-bold text-gray-900 group-hover:text-orange-600 transition-colors line-clamp-2 leading-snug tracking-tight"><%= item.display_title %></h3>
          </div>
        <% end %>
      <% end %>
    </div>
  <% end %>

  <%# ── Desktop Hero: featured story + marriage ad beside it ── %>
  <div class="hidden sm:grid sm:grid-cols-3 gap-4 lg:gap-5 mb-4 lg:mb-4 animate-fade-up">
    <%# Main hero — spans 2 cols %>
    <div class="col-span-2">
      <%= link_to featured_path, class: "group block rounded-2xl overflow-hidden relative lg:h-[420px] h-72 card-lift" do %>
        <% if @featured.cover_image.attached? %>
          <%= optimized_image_tag @featured.cover_image, variant: :hero, class: "w-full h-full object-cover img-zoom", loading: "eager" %>
        <% else %>
          <div class="w-full h-full gradient-hero"></div>
        <% end %>
        <div class="absolute inset-0" style="background: linear-gradient(0deg, rgba(0,0,0,0.85) 0%, rgba(0,0,0,0.35) 40%, rgba(0,0,0,0.1) 100%)"></div>
        <div class="absolute top-4 left-5 flex items-center gap-2.5">
          <span class="relative flex items-center justify-center h-3 w-3">
            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-orange-400 opacity-75"></span>
            <span class="relative inline-flex rounded-full h-2 w-2 bg-orange-500"></span>
          </span>
          <span class="text-[10px] font-bold px-2.5 py-0.5 rounded-md text-white/90 uppercase tracking-wider" style="background: rgba(249,115,22,0.7)"><%= featured_badge_label %></span>
        </div>
        <div class="absolute bottom-0 left-0 right-0 p-5 lg:p-8">
          <h2 class="text-xl lg:text-3xl font-black text-white leading-tight line-clamp-3 tracking-tight group-hover:text-orange-200 transition-colors drop-shadow-lg">
            <%= @featured.display_title %>
          </h2>
          <p class="text-sm text-white/60 line-clamp-2 leading-relaxed mt-2 lg:mt-3"><%= truncate(strip_tags(featured_content), length: 200) %></p>
        </div>
      <% end %>
    </div>

    <%# Marriage ad — 1 col, beside hero %>
    <div class="col-span-1">
      <%= render "shared/billboard_marriage_hero" %>
    </div>
  </div>
<% end %>
